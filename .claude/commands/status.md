Run the following commands and summarize my current workflow status:
1. `git status` - any uncommitted changes?
2. `git log --oneline -5` - recent commits and which branch I'm on
3. `gh pr status` - any open PRs and their CI check status

Then tell me clearly:
- Where I am in the workflow (editing / committed / PR open / CI running / ready to merge)
- What the next logical step is
